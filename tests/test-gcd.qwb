function int gcd(int a, int b){
  REPEAT until (a != b){
    if (a > b) a = a - b;
    otherwise b = b - a;
  }
  output a;
}

function int main(){
  print(gcd(2,14));
  print(gcd(3,15));
  print(gcd(99,121));
  output 0;
}
